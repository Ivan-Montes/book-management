import BookshopListContainer from '../containers/BookshopListContainer';

const Bookshops: React.FC = () => {
  return (
    <div className="container">
      <BookshopListContainer />
    </div>
  );
};

export default Bookshops;
